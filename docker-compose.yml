version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: astro-playground-frontend
    container_name: astro-playground-frontend
    ports:
      - "4321:4321"
    restart: unless-stopped